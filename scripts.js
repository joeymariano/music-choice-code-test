window.onload = function(){
	// API call to data.json

	// parse data 

	// create Show class
	// title imagUrl artist and rating

	// populate template information with info from first show
	// iterate over shows
	// copy previous divs and repopulate

	
	// fetch('/data.json') 
	//   .then((response) => {
	//     return response.json();
	//   })
	//   .then((myJson) => {
	//     console.log(myJson);
	//   });
}